#include <iostream>
#include "solarReadData.h"
#include <vector>

using namespace std;

vector<double> readWatts(string filename)
{
  //Declare vector to store power data
  vector<double> wattVec;

  //Get data file
  ifstream file (filename);

  while (file.good())
  {
    getline()
  }
}
